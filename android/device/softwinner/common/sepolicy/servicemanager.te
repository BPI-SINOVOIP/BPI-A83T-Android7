# servicemanager - the Binder context manager

allow servicemanager gpsd:dir rw_dir_perms;
allow servicemanager gpsd:file rw_file_perms;
allow servicemanager gpsd:process getattr;
allow servicemanager awinit:dir search;
allow servicemanager awinit:file{ read open };
allow servicemanager awinit:process getattr;
